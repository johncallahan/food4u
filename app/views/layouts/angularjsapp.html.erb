<!DOCTYPE html>
<html ng-app="Food4uClient" lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Food4u" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for dumb IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <meta id='csrf' ng-controller="CsrfCtrl".
      data-csrf="<%= form_authenticity_token%>">

    <!-- <link href="assets/favicon.ico" rel="shortcut icon"> -->
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Food4u</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <!-- main menu models -->
<li><%= link_to 'Restaurants', restaurants_path  %></li>
            </ul>
	    <ul class="nav pull-right">
<li><%= link_to 'logout', destroy_user_session_path, :method => :delete %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
          <!-- templates load here -->
          <div ng-view class="span4"></div>
	  <div class="span6" ng-controller="RestaurantsIndexCtrl">
	    <leaflet width="600px" height="480px"></leaflet>
	  </div>
          <!-- templates load here -->
            <div ng-controller="TagsIndexCtrl" class="span2">
              <div class="well sidebar-nav">
                <h3>Tags</h3>
		  <label ng-repeat="tag in tags">
                    <input id="{{tag.name}}" type="radio" value="{{tag.name}}" name="tagName" ng-model="$parent.currentTag"/>{{tag.name}}
                  </label>
              </div><!--/.well -->
            </div><!--/span-->
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div> <!-- /container -->
  </body>
</html>

